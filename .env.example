# PostgreSQL Database
DB_EXTERNAL_PORT=5432
DB_DATABASE=pauta_cidada
DB_USERNAME=postgres
DB_PASSWORD=postgres123
DB_HOST=database
DB_PORT=5432

# Frontend
VITE_API_URL=http://localhost:8000

# Python Backend (FastAPI)
PYTHON_BACKEND_PORT=8000

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_API_KEY_HERE

# Google Cloud BigQuery Configuration
GOOGLE_CLOUD_PROJECT=your-project-id

# OPÇÃO 1: Credenciais como JSON (RECOMENDADO PARA PORTAINER/DOCKER)
# Cole o conteúdo do seu credentials.json em uma única linha
# Para gerar: cat credentials.json | jq -c '.'
GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account","project_id":"your-project","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...@....iam.gserviceaccount.com",...}'

# OPÇÃO 2: Path para arquivo (DESENVOLVIMENTO LOCAL - descomente se preferir)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_BUCKET_NAME=proposition-pdfs

# Database URL (Supabase PostgreSQL)
# IMPORTANTE: Use o Connection Pooler para melhor performance e compatibilidade
# Formato: postgresql+asyncpg://postgres.PROJECT_REF:PASSWORD@REGION.pooler.supabase.com:5432/postgres
# Encontre seu Connection Pooler em: Supabase Dashboard → Settings → Database → Connection Pooler
# NOTA: asyncpg usa SSL por padrão, não é necessário adicionar parâmetros SSL na URL
DATABASE_URL=postgresql+asyncpg://postgres.your-project:your-password@aws-0-us-west-2.pooler.supabase.com:5432/postgres
